[project]
name = "reviewbot"
version = "0.1.0"
description = "Academic review quiz system"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.optional-dependencies]
server = ["flask>=3.0"]
extraction = ["marker-pdf>=0.2.0"]

[dependency-groups]
dev = ["flask>=3.0", "marker-pdf>=0.2.0"]
