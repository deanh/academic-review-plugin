[project]
name = "reviewbot"
version = "0.1.0"
description = "Academic review quiz system"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["flask>=3.0"]

[project.optional-dependencies]
extraction = ["marker-pdf>=0.2.0"]

[dependency-groups]
dev = ["marker-pdf>=0.2.0"]
